const unitsContainer = document.getElementById("units-container");
const unitsSelect = document.getElementById("units-select");

unitsContainer.addEventListener('click', function() {
    unitsSelect.style.display = "block";
})